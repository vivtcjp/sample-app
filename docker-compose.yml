# always preserve the code comments
version: '3.8'

services:
  app:
    build: .
    ports:
      # Dont change for this project 
      - "5454:5454"
    environment:
      - NODE_ENV=development
    volumes:
      - .:/usr/src/app
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: